from function_exercises import calculate_tip
calculate_tip(80, .30)
print (calculate_tip(80, .30))

from function_exercises import is_vowel
print (is_vowel("t"))

import itertools

print (list(itertools.combinations('ABCD', 2)))
print (list(itertools.combinations('abc', [1,2,3])))
